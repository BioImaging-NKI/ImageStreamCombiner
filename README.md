# Combine ImageStream Files

## Installation
### plain
`pip install -e .`
### with gui
`pip install -e .[ui]`
### for development
`pip install -e .[dev]`

## Building
* `pyi-makespec main_ui.py`
* `pyinstaller ImageStreamCombiner.spec`